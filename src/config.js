// Define a configuration for the panels
export const panelConfig = [
  {
    // Each panel has a text property, which is the text displayed on the panel
    text: 'Message',
    // Each panel also has a node property, which is the type of node that the panel represents
    node: 'messageNode',
  },
]
